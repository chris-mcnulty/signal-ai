{
  "name": "@workspace/api-spec",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "codegen": "node -e \"try{require('fs').unlinkSync('../../lib/api-zod/src/index.ts')}catch(_){}\" && orval --config ./orval.config.ts && pnpm -w run typecheck:libs"
  },
  "devDependencies": {
    "orval": "^8.20.0"
  }
}
